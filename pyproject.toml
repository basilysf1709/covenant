[project]
name = "covenant"
version = "0.1.0"
description = "Covenant CLI tool"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0",
]

[project.scripts]
covenant = "cli:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
